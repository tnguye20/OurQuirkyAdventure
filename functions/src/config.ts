const CriteriaMapper = {
  tags: 'tags',
  cities: 'city',
  states: 'state',
  takenMonths: 'takenMonth',
  takenYears: 'takenYear',
  categories: 'category',
  fromDate: 'fromDate',
  toDate: 'toDate'
}

const config = {
  CriteriaMapper
};

export default config;